import * as React from 'react';

const Chrono = ()=>{

    return (
        <div>
            <h3>Chronometer</h3>
            <div>
                <p>
                    <span>time</span>
                </p>
            </div>
            <button>Adds multiple AMRAPs</button>
            <button>START</button>
        </div>
    )
};

export default Chrono;